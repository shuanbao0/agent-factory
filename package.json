{
  "name": "agent-factory",
  "version": "0.4.49",
  "description": "AI Employee Factory — Self-contained multi-agent team with built-in OpenClaw engine",
  "type": "module",
  "bin": {
    "agent-factory": "bin/agent-factory.mjs"
  },
  "scripts": {
    "start": "node scripts/start.mjs",
    "gateway": "node scripts/start.mjs --gateway-only",
    "ui": "cd ui && npm run dev",
    "postinstall": "node scripts/patch-openclaw.mjs",
    "test": "node --test 'shared/**/*.test.cjs'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "clawhub": "^0.7.0",
    "openclaw": "^2026.3.13",
    "ws": "^8.19.0"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "license": "GPL-3.0-only"
}
